export const ImagePlaceholder = () => {
  return (
    <div
      style={{
        width: "100%",
        minHeight: 180,
        borderRadius: 12,
        border: "1px dashed var(--border)",
        background: "#f9fafb",
        display: "grid",
        placeItems: "center",
        color: "var(--muted)",
        fontWeight: 600,
      }}
    >
      Imagem futura aqui
    </div>
  );
};
